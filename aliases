alias l="exa -lh --git"
alias a="exa -lha --git"
alias c='clear'
alias v="nvim"
alias vi="echo 'Use v'"
alias o="open"
alias ls="exa --git"
alias tree="ls --tree"
alias cat="bat"
alias k="kubectl"

if [[ $OSTYPE == *"linux"* ]]; then
  alias pbcopy="xsel --clipboard"
  alias pbpaste="xsel --clipboard"
  alias open="xdg-open"
fi

alias d="cd ~/code/personal/dotfiles"
alias reload="source ~/.zshrc"
alias path='echo $PATH | tr ":" "\n"'

alias g="git"
alias gbc='git checkout $(git branch -a | cut -c 3- | fzf)'

alias genkeys='ssh-keygen -t rsa -b 4096 -C "skippednote@skippednote.dev"'
alias copykeys='cat ~/.ssh/id_rsa.pub | pbcopy'

alias vu='vagrant up'
alias vs='vagrant ssh'
alias vh='vagrant halt'
alias vp='vagrant provision'

alias yt='docker run --rm -u $(id -u):$(id -g) -v $PWD:/data vimagick/youtube-dl'
