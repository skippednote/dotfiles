# Deployed to ~/.gitignore (core.excludesfile). Deliberately not named
# .gitignore: inside home/ that would become an active gitignore governing
# this whole tree, and these patterns would start hiding tracked files.
# nix/modules/home.nix maps it to the dotted name.

.DS_Store
.AppleDouble
.LSOverride
._*

node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.venv
venv/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

.idea/
*.swp
*.swo
*~

*.tmp
*.temp
*.log

.claude/settings.local.json

**/.claude/settings.local.json
